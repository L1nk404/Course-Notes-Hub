## Converting C++ into a program

1. `main.cpp` => produces assembler: `g++ -S main.cpp`
2. `main.cpp` => produces object code: `g++ -c main.cpp`
3. `main.cpp` => compile, assemble and link: `g++ main.cpp`
